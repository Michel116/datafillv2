
// This component will render null, so no loading indicator will be shown
// during page transitions. The initial app load animation is handled in layout.tsx.
export default function Loading() {
  return null;
}
